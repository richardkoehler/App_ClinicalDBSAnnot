Fix `install.ps1` when run with `iex`: move install into a nested function so `$PSCmdlet` binds; document `iex` vs `-AddDesktopShortcut` (env `DBS_ANNOTATOR_ADD_DESKTOP` or `& ([scriptblock]::Create((iwr …).Content))`).
